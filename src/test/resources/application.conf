# this file is only required for tests, where we actually start a Ktor web service ourselves
ktor {
  application {
    modules = [com.booleworks.kjobs.common.ApplicationKt.module]
  }
  deployment {
    port = 8080
    connectionIdleTimeoutSeconds = 3600
  }
}
